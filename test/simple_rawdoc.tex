\documentclass{ctexart}
\pagestyle{plain}
\ctexset{
    section = {name = {,、}, number = \chinese{section}},
    subsection = {name = {（,）}, number = \chinese{subsection}},
    subsubsection = {number = \arabic{subsubsection}}
}

\begin{document}

\section{测试转换}

\subsection{测试标题}

这是一个测试文档，用于验证 Markdown 转 LaTeX 的转换功能。这是一个段落，包含一些\textbf{加粗}和\textit{斜体}文本，以及一个行内代码示例 \texttt{let x = 10;}。
\begin{verbatim}
fn main() {
    println!("Hello, world!");
}
\end{verbatim}

这是行内代码示例：\texttt{let x = 10;}，应该被正确转换为 LaTeX 的 \texttt{\textbackslash{}texttt\{let x = 10;\}}。这是一个列表：
\begin{itemize}
\item 项目一
\item 项目二
\item 项目三
\end{itemize}



\end{document}
